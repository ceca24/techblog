<div class="card w-100 wb-3" style="width: 18rem;">
    <div class="card-body">
        <h5 class="card-title">{{this.title}}</h5>
        <h6 class="card-subtitle wb-2 text-muted">Created: {{format_date this.created_at}}</h6>
        <p class="card-text">{{this.post_body}}</p>
    </div>