<div class="comments">
    {{#each this}}
    <div>
        <p class="card-body2">
            {{comment_body}} - {{user.username}} - {{format_date created_at}}
        </p>
    </div>
    {{/each}}
</div>